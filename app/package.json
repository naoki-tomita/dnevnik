{
  "dependencies": {
    "zheleznaya": "0.1.12"
  },
  "devDependencies": {
    "npm-run-all": "latest",
    "parcel": "latest",
    "sass": "latest",
    "typescript": "latest"
  },
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "run-p watch serve",
    "watch": "parcel watch index.html",
    "serve": "npx http-server dist -p 80 --proxy http://localhost:8000",
    "build:production": "parcel build index.html"
  }
}
